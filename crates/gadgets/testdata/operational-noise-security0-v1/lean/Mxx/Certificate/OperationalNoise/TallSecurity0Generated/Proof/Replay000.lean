import Mxx.Certificate.OperationalNoise.TallSecurity0Generated.Proof.History

set_option autoImplicit false
set_option relaxedAutoImplicit false

namespace Mxx.Certificate.OperationalNoise.TallSecurity0Generated.Proof.Replay000

open Mxx.Certificate.OperationalNoise
open CertificateABI
open Mxx.Certificate.OperationalNoise.TallSecurity0Generated

def replayState0 : ReplayState := ⟨0, []⟩
def replayState1 : ReplayState := ⟨4, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState2 : ReplayState := ⟨8, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState3 : ReplayState := ⟨12, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState4 : ReplayState := ⟨16, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState5 : ReplayState := ⟨20, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState6 : ReplayState := ⟨24, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState7 : ReplayState := ⟨28, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState8 : ReplayState := ⟨32, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState9 : ReplayState := ⟨36, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState10 : ReplayState := ⟨40, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState11 : ReplayState := ⟨44, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState12 : ReplayState := ⟨48, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState13 : ReplayState := ⟨52, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState14 : ReplayState := ⟨56, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState15 : ReplayState := ⟨60, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState16 : ReplayState := ⟨64, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState17 : ReplayState := ⟨68, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState18 : ReplayState := ⟨72, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState19 : ReplayState := ⟨76, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState20 : ReplayState := ⟨80, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState21 : ReplayState := ⟨84, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState22 : ReplayState := ⟨88, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState23 : ReplayState := ⟨92, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState24 : ReplayState := ⟨96, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState25 : ReplayState := ⟨100, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState26 : ReplayState := ⟨104, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState27 : ReplayState := ⟨108, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState28 : ReplayState := ⟨112, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState29 : ReplayState := ⟨116, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState30 : ReplayState := ⟨120, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState31 : ReplayState := ⟨124, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState32 : ReplayState := ⟨128, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState33 : ReplayState := ⟨132, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState34 : ReplayState := ⟨136, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState35 : ReplayState := ⟨140, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState36 : ReplayState := ⟨144, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState37 : ReplayState := ⟨148, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState38 : ReplayState := ⟨152, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState39 : ReplayState := ⟨156, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState40 : ReplayState := ⟨160, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState41 : ReplayState := ⟨164, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState42 : ReplayState := ⟨168, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState43 : ReplayState := ⟨172, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState44 : ReplayState := ⟨176, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState45 : ReplayState := ⟨180, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState46 : ReplayState := ⟨184, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState47 : ReplayState := ⟨188, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState48 : ReplayState := ⟨192, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState49 : ReplayState := ⟨196, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState50 : ReplayState := ⟨200, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState51 : ReplayState := ⟨204, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState52 : ReplayState := ⟨208, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState53 : ReplayState := ⟨212, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState54 : ReplayState := ⟨216, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState55 : ReplayState := ⟨220, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState56 : ReplayState := ⟨224, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState57 : ReplayState := ⟨228, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState58 : ReplayState := ⟨232, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState59 : ReplayState := ⟨236, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState60 : ReplayState := ⟨240, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState61 : ReplayState := ⟨244, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState62 : ReplayState := ⟨248, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState63 : ReplayState := ⟨252, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayState64 : ReplayState := ⟨256, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

def replayEventState1 : ReplayState := ⟨1, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState2 : ReplayState := ⟨2, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState3 : ReplayState := ⟨3, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep0 : stepAt document history replayState0 = some replayEventState1 := by
  rfl

theorem replayStep1 : stepAt document history replayEventState1 = some replayEventState2 := by
  rfl

theorem replayStep2 : stepAt document history replayEventState2 = some replayEventState3 := by
  apply stepAt_resultExact document history replayEventState2
    (⟨.program ⟨214⟩, ⟨6544⟩⟩) Proof.Events000.exact2RawTerms .large 1 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact2RawTermsValid]
    rfl

theorem replayStep3 : stepAt document history replayEventState3 = some replayState1 := by
  rfl

theorem replayChunk0 : ReplayChain document history replayState0 replayState1 :=
  .chunk 4 (by
    unfold replayRange
    rw [show replayState0.cursor = 0 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (0 ≤ 4) && decide (4 ≤ 107567) &&
        decide (4 - 0 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState0.cursor = 0 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (4 - 0) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep0]
    simp only [Option.bind_some]
    rw [replayStep1]
    simp only [Option.bind_some]
    rw [replayStep2]
    simp only [Option.bind_some]
    rw [replayStep3]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk1 : ReplayChain document history replayState1 replayState2 :=
  .chunk 8 (by rfl)

theorem replayChunk2 : ReplayChain document history replayState2 replayState3 :=
  .chunk 12 (by rfl)

theorem replayChunk3 : ReplayChain document history replayState3 replayState4 :=
  .chunk 16 (by rfl)

theorem replayChunk4 : ReplayChain document history replayState4 replayState5 :=
  .chunk 20 (by rfl)

def replayEventState21 : ReplayState := ⟨21, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState22 : ReplayState := ⟨22, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState23 : ReplayState := ⟨23, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep20 : stepAt document history replayState5 = some replayEventState21 := by
  apply stepAt_resultExact document history replayState5
    (⟨.program ⟨214⟩, ⟨0⟩⟩) Proof.Events000.exact20RawTerms (.finite 1) 19 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact20RawTermsValid]
    rfl

theorem replayStep21 : stepAt document history replayEventState21 = some replayEventState22 := by
  rfl

theorem replayStep22 : stepAt document history replayEventState22 = some replayEventState23 := by
  rfl

theorem replayStep23 : stepAt document history replayEventState23 = some replayState6 := by
  rfl

theorem replayChunk5 : ReplayChain document history replayState5 replayState6 :=
  .chunk 24 (by
    unfold replayRange
    rw [show replayState5.cursor = 20 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (20 ≤ 24) && decide (24 ≤ 107567) &&
        decide (24 - 20 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState5.cursor = 20 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (24 - 20) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep20]
    simp only [Option.bind_some]
    rw [replayStep21]
    simp only [Option.bind_some]
    rw [replayStep22]
    simp only [Option.bind_some]
    rw [replayStep23]
    simp only [Option.bind_some]
    rfl)

def replayEventState25 : ReplayState := ⟨25, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState26 : ReplayState := ⟨26, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState27 : ReplayState := ⟨27, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep24 : stepAt document history replayState6 = some replayEventState25 := by
  rfl

theorem replayStep25 : stepAt document history replayEventState25 = some replayEventState26 := by
  rfl

theorem replayStep26 : stepAt document history replayEventState26 = some replayEventState27 := by
  rfl

theorem replayStep27 : stepAt document history replayEventState27 = some replayState7 := by
  apply stepAt_resultExact document history replayEventState27
    (⟨.program ⟨214⟩, ⟨5506⟩⟩) Proof.Events000.exact27RawTerms (.finite 1) 26 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact27RawTermsValid]
    rfl

theorem replayChunk6 : ReplayChain document history replayState6 replayState7 :=
  .chunk 28 (by
    unfold replayRange
    rw [show replayState6.cursor = 24 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (24 ≤ 28) && decide (28 ≤ 107567) &&
        decide (28 - 24 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState6.cursor = 24 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (28 - 24) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep24]
    simp only [Option.bind_some]
    rw [replayStep25]
    simp only [Option.bind_some]
    rw [replayStep26]
    simp only [Option.bind_some]
    rw [replayStep27]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk7 : ReplayChain document history replayState7 replayState8 :=
  .chunk 32 (by rfl)

def replayEventState33 : ReplayState := ⟨33, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState34 : ReplayState := ⟨34, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState35 : ReplayState := ⟨35, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep32 : stepAt document history replayState8 = some replayEventState33 := by
  apply stepAt_resultExact document history replayState8
    (⟨.program ⟨214⟩, ⟨6564⟩⟩) Proof.Events000.exact32RawTerms .large 30 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact32RawTermsValid]
    rfl

theorem replayStep33 : stepAt document history replayEventState33 = some replayEventState34 := by
  rfl

theorem replayStep34 : stepAt document history replayEventState34 = some replayEventState35 := by
  apply stepAt_resultExact document history replayEventState34
    (⟨.program ⟨214⟩, ⟨6396⟩⟩) Proof.Events000.exact34RawTerms (.finite 17653518570535758778568050596964732841621436071473508005967) 33 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact34RawTermsValid]
    rfl

theorem replayStep35 : stepAt document history replayEventState35 = some replayState9 := by
  rfl

theorem replayChunk8 : ReplayChain document history replayState8 replayState9 :=
  .chunk 36 (by
    unfold replayRange
    rw [show replayState8.cursor = 32 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (32 ≤ 36) && decide (36 ≤ 107567) &&
        decide (36 - 32 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState8.cursor = 32 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (36 - 32) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep32]
    simp only [Option.bind_some]
    rw [replayStep33]
    simp only [Option.bind_some]
    rw [replayStep34]
    simp only [Option.bind_some]
    rw [replayStep35]
    simp only [Option.bind_some]
    rfl)

def replayEventState37 : ReplayState := ⟨37, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState38 : ReplayState := ⟨38, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState39 : ReplayState := ⟨39, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep36 : stepAt document history replayState9 = some replayEventState37 := by
  apply stepAt_resultExact document history replayState9
    (⟨.program ⟨214⟩, ⟨6410⟩⟩) Proof.Events000.exact36RawTerms (.finite 234576762718813941966540) 35 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact36RawTermsValid]
    rfl

theorem replayStep37 : stepAt document history replayEventState37 = some replayEventState38 := by
  rfl

theorem replayStep38 : stepAt document history replayEventState38 = some replayEventState39 := by
  rfl

theorem replayStep39 : stepAt document history replayEventState39 = some replayState10 := by
  rfl

theorem replayChunk9 : ReplayChain document history replayState9 replayState10 :=
  .chunk 40 (by
    unfold replayRange
    rw [show replayState9.cursor = 36 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (36 ≤ 40) && decide (40 ≤ 107567) &&
        decide (40 - 36 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState9.cursor = 36 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (40 - 36) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep36]
    simp only [Option.bind_some]
    rw [replayStep37]
    simp only [Option.bind_some]
    rw [replayStep38]
    simp only [Option.bind_some]
    rw [replayStep39]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk10 : ReplayChain document history replayState10 replayState11 :=
  .chunk 44 (by rfl)

theorem replayChunk11 : ReplayChain document history replayState11 replayState12 :=
  .chunk 48 (by rfl)

def replayEventState49 : ReplayState := ⟨49, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState50 : ReplayState := ⟨50, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState51 : ReplayState := ⟨51, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep48 : stepAt document history replayState12 = some replayEventState49 := by
  rfl

theorem replayStep49 : stepAt document history replayEventState49 = some replayEventState50 := by
  rfl

theorem replayStep50 : stepAt document history replayEventState50 = some replayEventState51 := by
  rfl

theorem replayStep51 : stepAt document history replayEventState51 = some replayState13 := by
  apply stepAt_resultExact document history replayEventState51
    (⟨.program ⟨214⟩, ⟨13382⟩⟩) Proof.Events000.exact51RawTerms (.finite 60) 50 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact51RawTermsValid]
    rfl

theorem replayChunk12 : ReplayChain document history replayState12 replayState13 :=
  .chunk 52 (by
    unfold replayRange
    rw [show replayState12.cursor = 48 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (48 ≤ 52) && decide (52 ≤ 107567) &&
        decide (52 - 48 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState12.cursor = 48 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (52 - 48) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep48]
    simp only [Option.bind_some]
    rw [replayStep49]
    simp only [Option.bind_some]
    rw [replayStep50]
    simp only [Option.bind_some]
    rw [replayStep51]
    simp only [Option.bind_some]
    rfl)

def replayEventState53 : ReplayState := ⟨53, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState54 : ReplayState := ⟨54, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState55 : ReplayState := ⟨55, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep52 : stepAt document history replayState13 = some replayEventState53 := by
  rfl

theorem replayStep53 : stepAt document history replayEventState53 = some replayEventState54 := by
  rfl

theorem replayStep54 : stepAt document history replayEventState54 = some replayEventState55 := by
  apply stepAt_resultExact document history replayEventState54
    (⟨.program ⟨214⟩, ⟨10365⟩⟩) Proof.Events000.exact54RawTerms (.finite 60) 53 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact54RawTermsValid]
    rfl

theorem replayStep55 : stepAt document history replayEventState55 = some replayState14 := by
  rfl

theorem replayChunk13 : ReplayChain document history replayState13 replayState14 :=
  .chunk 56 (by
    unfold replayRange
    rw [show replayState13.cursor = 52 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (52 ≤ 56) && decide (56 ≤ 107567) &&
        decide (56 - 52 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState13.cursor = 52 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (56 - 52) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep52]
    simp only [Option.bind_some]
    rw [replayStep53]
    simp only [Option.bind_some]
    rw [replayStep54]
    simp only [Option.bind_some]
    rw [replayStep55]
    simp only [Option.bind_some]
    rfl)

def replayEventState57 : ReplayState := ⟨57, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState58 : ReplayState := ⟨58, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState59 : ReplayState := ⟨59, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep56 : stepAt document history replayState14 = some replayEventState57 := by
  rfl

theorem replayStep57 : stepAt document history replayEventState57 = some replayEventState58 := by
  rfl

theorem replayStep58 : stepAt document history replayEventState58 = some replayEventState59 := by
  rfl

theorem replayStep59 : stepAt document history replayEventState59 = some replayState15 := by
  apply stepAt_resultExact document history replayEventState59
    (⟨.program ⟨214⟩, ⟨13383⟩⟩) Proof.Events000.exact59RawTerms (.finite 3600) 57 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact59RawTermsValid]
    rfl

theorem replayChunk14 : ReplayChain document history replayState14 replayState15 :=
  .chunk 60 (by
    unfold replayRange
    rw [show replayState14.cursor = 56 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (56 ≤ 60) && decide (60 ≤ 107567) &&
        decide (60 - 56 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState14.cursor = 56 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (60 - 56) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep56]
    simp only [Option.bind_some]
    rw [replayStep57]
    simp only [Option.bind_some]
    rw [replayStep58]
    simp only [Option.bind_some]
    rw [replayStep59]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk15 : ReplayChain document history replayState15 replayState16 :=
  .chunk 64 (by rfl)

def replayEventState65 : ReplayState := ⟨65, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState66 : ReplayState := ⟨66, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState67 : ReplayState := ⟨67, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep64 : stepAt document history replayState16 = some replayEventState65 := by
  rfl

theorem replayStep65 : stepAt document history replayEventState65 = some replayEventState66 := by
  apply stepAt_resultExact document history replayEventState65
    (⟨.program ⟨214⟩, ⟨17027⟩⟩) Proof.Events000.exact65RawTerms (.finite 60) 64 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact65RawTermsValid]
    rfl

theorem replayStep66 : stepAt document history replayEventState66 = some replayEventState67 := by
  rfl

theorem replayStep67 : stepAt document history replayEventState67 = some replayState17 := by
  rfl

theorem replayChunk16 : ReplayChain document history replayState16 replayState17 :=
  .chunk 68 (by
    unfold replayRange
    rw [show replayState16.cursor = 64 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (64 ≤ 68) && decide (68 ≤ 107567) &&
        decide (68 - 64 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState16.cursor = 64 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (68 - 64) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep64]
    simp only [Option.bind_some]
    rw [replayStep65]
    simp only [Option.bind_some]
    rw [replayStep66]
    simp only [Option.bind_some]
    rw [replayStep67]
    simp only [Option.bind_some]
    rfl)

def replayEventState69 : ReplayState := ⟨69, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState70 : ReplayState := ⟨70, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState71 : ReplayState := ⟨71, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep68 : stepAt document history replayState17 = some replayEventState69 := by
  rfl

theorem replayStep69 : stepAt document history replayEventState69 = some replayEventState70 := by
  rfl

theorem replayStep70 : stepAt document history replayEventState70 = some replayEventState71 := by
  rfl

theorem replayStep71 : stepAt document history replayEventState71 = some replayState18 := by
  apply stepAt_resultExact document history replayEventState71
    (⟨.program ⟨214⟩, ⟨18182⟩⟩) Proof.Events000.exact71RawTerms (.finite 63) 70 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact71RawTermsValid]
    rfl

theorem replayChunk17 : ReplayChain document history replayState17 replayState18 :=
  .chunk 72 (by
    unfold replayRange
    rw [show replayState17.cursor = 68 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (68 ≤ 72) && decide (72 ≤ 107567) &&
        decide (72 - 68 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState17.cursor = 68 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (72 - 68) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep68]
    simp only [Option.bind_some]
    rw [replayStep69]
    simp only [Option.bind_some]
    rw [replayStep70]
    simp only [Option.bind_some]
    rw [replayStep71]
    simp only [Option.bind_some]
    rfl)

def replayEventState73 : ReplayState := ⟨73, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState74 : ReplayState := ⟨74, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState75 : ReplayState := ⟨75, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep72 : stepAt document history replayState18 = some replayEventState73 := by
  rfl

theorem replayStep73 : stepAt document history replayEventState73 = some replayEventState74 := by
  rfl

theorem replayStep74 : stepAt document history replayEventState74 = some replayEventState75 := by
  apply stepAt_resultExact document history replayEventState74
    (⟨.program ⟨214⟩, ⟨13186⟩⟩) Proof.Events000.exact74RawTerms (.finite 58) 73 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact74RawTermsValid]
    rfl

theorem replayStep75 : stepAt document history replayEventState75 = some replayState19 := by
  rfl

theorem replayChunk18 : ReplayChain document history replayState18 replayState19 :=
  .chunk 76 (by
    unfold replayRange
    rw [show replayState18.cursor = 72 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (72 ≤ 76) && decide (76 ≤ 107567) &&
        decide (76 - 72 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState18.cursor = 72 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (76 - 72) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep72]
    simp only [Option.bind_some]
    rw [replayStep73]
    simp only [Option.bind_some]
    rw [replayStep74]
    simp only [Option.bind_some]
    rw [replayStep75]
    simp only [Option.bind_some]
    rfl)

def replayEventState77 : ReplayState := ⟨77, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState78 : ReplayState := ⟨78, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState79 : ReplayState := ⟨79, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep76 : stepAt document history replayState19 = some replayEventState77 := by
  rfl

theorem replayStep77 : stepAt document history replayEventState77 = some replayEventState78 := by
  apply stepAt_resultExact document history replayEventState77
    (⟨.program ⟨214⟩, ⟨10260⟩⟩) Proof.Events000.exact77RawTerms (.finite 58) 76 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact77RawTermsValid]
    rfl

theorem replayStep78 : stepAt document history replayEventState78 = some replayEventState79 := by
  rfl

theorem replayStep79 : stepAt document history replayEventState79 = some replayState20 := by
  rfl

theorem replayChunk19 : ReplayChain document history replayState19 replayState20 :=
  .chunk 80 (by
    unfold replayRange
    rw [show replayState19.cursor = 76 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (76 ≤ 80) && decide (80 ≤ 107567) &&
        decide (80 - 76 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState19.cursor = 76 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (80 - 76) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep76]
    simp only [Option.bind_some]
    rw [replayStep77]
    simp only [Option.bind_some]
    rw [replayStep78]
    simp only [Option.bind_some]
    rw [replayStep79]
    simp only [Option.bind_some]
    rfl)

def replayEventState81 : ReplayState := ⟨81, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState82 : ReplayState := ⟨82, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState83 : ReplayState := ⟨83, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep80 : stepAt document history replayState20 = some replayEventState81 := by
  rfl

theorem replayStep81 : stepAt document history replayEventState81 = some replayEventState82 := by
  rfl

theorem replayStep82 : stepAt document history replayEventState82 = some replayEventState83 := by
  apply stepAt_resultExact document history replayEventState82
    (⟨.program ⟨214⟩, ⟨13187⟩⟩) Proof.Events000.exact82RawTerms (.finite 3364) 80 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact82RawTermsValid]
    rfl

theorem replayStep83 : stepAt document history replayEventState83 = some replayState21 := by
  rfl

theorem replayChunk20 : ReplayChain document history replayState20 replayState21 :=
  .chunk 84 (by
    unfold replayRange
    rw [show replayState20.cursor = 80 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (80 ≤ 84) && decide (84 ≤ 107567) &&
        decide (84 - 80 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState20.cursor = 80 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (84 - 80) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep80]
    simp only [Option.bind_some]
    rw [replayStep81]
    simp only [Option.bind_some]
    rw [replayStep82]
    simp only [Option.bind_some]
    rw [replayStep83]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk21 : ReplayChain document history replayState21 replayState22 :=
  .chunk 88 (by rfl)

def replayEventState89 : ReplayState := ⟨89, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState90 : ReplayState := ⟨90, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState91 : ReplayState := ⟨91, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep88 : stepAt document history replayState22 = some replayEventState89 := by
  apply stepAt_resultExact document history replayState22
    (⟨.program ⟨214⟩, ⟨16887⟩⟩) Proof.Events000.exact88RawTerms (.finite 58) 87 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact88RawTermsValid]
    rfl

theorem replayStep89 : stepAt document history replayEventState89 = some replayEventState90 := by
  rfl

theorem replayStep90 : stepAt document history replayEventState90 = some replayEventState91 := by
  rfl

theorem replayStep91 : stepAt document history replayEventState91 = some replayState23 := by
  rfl

theorem replayChunk22 : ReplayChain document history replayState22 replayState23 :=
  .chunk 92 (by
    unfold replayRange
    rw [show replayState22.cursor = 88 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (88 ≤ 92) && decide (92 ≤ 107567) &&
        decide (92 - 88 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState22.cursor = 88 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (92 - 88) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep88]
    simp only [Option.bind_some]
    rw [replayStep89]
    simp only [Option.bind_some]
    rw [replayStep90]
    simp only [Option.bind_some]
    rw [replayStep91]
    simp only [Option.bind_some]
    rfl)

def replayEventState93 : ReplayState := ⟨93, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState94 : ReplayState := ⟨94, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState95 : ReplayState := ⟨95, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep92 : stepAt document history replayState23 = some replayEventState93 := by
  rfl

theorem replayStep93 : stepAt document history replayEventState93 = some replayEventState94 := by
  rfl

theorem replayStep94 : stepAt document history replayEventState94 = some replayEventState95 := by
  apply stepAt_resultExact document history replayEventState94
    (⟨.program ⟨214⟩, ⟨17097⟩⟩) Proof.Events000.exact94RawTerms (.finite 63) 93 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact94RawTermsValid]
    rfl

theorem replayStep95 : stepAt document history replayEventState95 = some replayState24 := by
  rfl

theorem replayChunk23 : ReplayChain document history replayState23 replayState24 :=
  .chunk 96 (by
    unfold replayRange
    rw [show replayState23.cursor = 92 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (92 ≤ 96) && decide (96 ≤ 107567) &&
        decide (96 - 92 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState23.cursor = 92 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (96 - 92) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep92]
    simp only [Option.bind_some]
    rw [replayStep93]
    simp only [Option.bind_some]
    rw [replayStep94]
    simp only [Option.bind_some]
    rw [replayStep95]
    simp only [Option.bind_some]
    rfl)

def replayEventState97 : ReplayState := ⟨97, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState98 : ReplayState := ⟨98, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState99 : ReplayState := ⟨99, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep96 : stepAt document history replayState24 = some replayEventState97 := by
  rfl

theorem replayStep97 : stepAt document history replayEventState97 = some replayEventState98 := by
  apply stepAt_resultExact document history replayEventState97
    (⟨.program ⟨214⟩, ⟨12990⟩⟩) Proof.Events000.exact97RawTerms (.finite 52) 96 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact97RawTermsValid]
    rfl

theorem replayStep98 : stepAt document history replayEventState98 = some replayEventState99 := by
  rfl

theorem replayStep99 : stepAt document history replayEventState99 = some replayState25 := by
  rfl

theorem replayChunk24 : ReplayChain document history replayState24 replayState25 :=
  .chunk 100 (by
    unfold replayRange
    rw [show replayState24.cursor = 96 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (96 ≤ 100) && decide (100 ≤ 107567) &&
        decide (100 - 96 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState24.cursor = 96 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (100 - 96) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep96]
    simp only [Option.bind_some]
    rw [replayStep97]
    simp only [Option.bind_some]
    rw [replayStep98]
    simp only [Option.bind_some]
    rw [replayStep99]
    simp only [Option.bind_some]
    rfl)

def replayEventState101 : ReplayState := ⟨101, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState102 : ReplayState := ⟨102, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState103 : ReplayState := ⟨103, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep100 : stepAt document history replayState25 = some replayEventState101 := by
  apply stepAt_resultExact document history replayState25
    (⟨.program ⟨214⟩, ⟨10155⟩⟩) Proof.Events000.exact100RawTerms (.finite 52) 99 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact100RawTermsValid]
    rfl

theorem replayStep101 : stepAt document history replayEventState101 = some replayEventState102 := by
  rfl

theorem replayStep102 : stepAt document history replayEventState102 = some replayEventState103 := by
  rfl

theorem replayStep103 : stepAt document history replayEventState103 = some replayState26 := by
  rfl

theorem replayChunk25 : ReplayChain document history replayState25 replayState26 :=
  .chunk 104 (by
    unfold replayRange
    rw [show replayState25.cursor = 100 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (100 ≤ 104) && decide (104 ≤ 107567) &&
        decide (104 - 100 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState25.cursor = 100 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (104 - 100) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep100]
    simp only [Option.bind_some]
    rw [replayStep101]
    simp only [Option.bind_some]
    rw [replayStep102]
    simp only [Option.bind_some]
    rw [replayStep103]
    simp only [Option.bind_some]
    rfl)

def replayEventState105 : ReplayState := ⟨105, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState106 : ReplayState := ⟨106, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState107 : ReplayState := ⟨107, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep104 : stepAt document history replayState26 = some replayEventState105 := by
  rfl

theorem replayStep105 : stepAt document history replayEventState105 = some replayEventState106 := by
  apply stepAt_resultExact document history replayEventState105
    (⟨.program ⟨214⟩, ⟨12991⟩⟩) Proof.Events000.exact105RawTerms (.finite 2704) 103 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact105RawTermsValid]
    rfl

theorem replayStep106 : stepAt document history replayEventState106 = some replayEventState107 := by
  rfl

theorem replayStep107 : stepAt document history replayEventState107 = some replayState27 := by
  rfl

theorem replayChunk26 : ReplayChain document history replayState26 replayState27 :=
  .chunk 108 (by
    unfold replayRange
    rw [show replayState26.cursor = 104 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (104 ≤ 108) && decide (108 ≤ 107567) &&
        decide (108 - 104 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState26.cursor = 104 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (108 - 104) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep104]
    simp only [Option.bind_some]
    rw [replayStep105]
    simp only [Option.bind_some]
    rw [replayStep106]
    simp only [Option.bind_some]
    rw [replayStep107]
    simp only [Option.bind_some]
    rfl)

def replayEventState109 : ReplayState := ⟨109, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState110 : ReplayState := ⟨110, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState111 : ReplayState := ⟨111, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep108 : stepAt document history replayState27 = some replayEventState109 := by
  rfl

theorem replayStep109 : stepAt document history replayEventState109 = some replayEventState110 := by
  rfl

theorem replayStep110 : stepAt document history replayEventState110 = some replayEventState111 := by
  rfl

theorem replayStep111 : stepAt document history replayEventState111 = some replayState28 := by
  apply stepAt_resultExact document history replayEventState111
    (⟨.program ⟨214⟩, ⟨16768⟩⟩) Proof.Events000.exact111RawTerms (.finite 52) 110 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact111RawTermsValid]
    rfl

theorem replayChunk27 : ReplayChain document history replayState27 replayState28 :=
  .chunk 112 (by
    unfold replayRange
    rw [show replayState27.cursor = 108 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (108 ≤ 112) && decide (112 ≤ 107567) &&
        decide (112 - 108 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState27.cursor = 108 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (112 - 108) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep108]
    simp only [Option.bind_some]
    rw [replayStep109]
    simp only [Option.bind_some]
    rw [replayStep110]
    simp only [Option.bind_some]
    rw [replayStep111]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk28 : ReplayChain document history replayState28 replayState29 :=
  .chunk 116 (by rfl)

def replayEventState117 : ReplayState := ⟨117, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState118 : ReplayState := ⟨118, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState119 : ReplayState := ⟨119, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep116 : stepAt document history replayState29 = some replayEventState117 := by
  rfl

theorem replayStep117 : stepAt document history replayEventState117 = some replayEventState118 := by
  apply stepAt_resultExact document history replayEventState117
    (⟨.program ⟨214⟩, ⟨16810⟩⟩) Proof.Events000.exact117RawTerms (.finite 63) 116 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact117RawTermsValid]
    rfl

theorem replayStep118 : stepAt document history replayEventState118 = some replayEventState119 := by
  rfl

theorem replayStep119 : stepAt document history replayEventState119 = some replayState30 := by
  rfl

theorem replayChunk29 : ReplayChain document history replayState29 replayState30 :=
  .chunk 120 (by
    unfold replayRange
    rw [show replayState29.cursor = 116 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (116 ≤ 120) && decide (120 ≤ 107567) &&
        decide (120 - 116 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState29.cursor = 116 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (120 - 116) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep116]
    simp only [Option.bind_some]
    rw [replayStep117]
    simp only [Option.bind_some]
    rw [replayStep118]
    simp only [Option.bind_some]
    rw [replayStep119]
    simp only [Option.bind_some]
    rfl)

def replayEventState121 : ReplayState := ⟨121, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState122 : ReplayState := ⟨122, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState123 : ReplayState := ⟨123, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep120 : stepAt document history replayState30 = some replayEventState121 := by
  apply stepAt_resultExact document history replayState30
    (⟨.program ⟨214⟩, ⟨12794⟩⟩) Proof.Events000.exact120RawTerms (.finite 46) 119 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact120RawTermsValid]
    rfl

theorem replayStep121 : stepAt document history replayEventState121 = some replayEventState122 := by
  rfl

theorem replayStep122 : stepAt document history replayEventState122 = some replayEventState123 := by
  rfl

theorem replayStep123 : stepAt document history replayEventState123 = some replayState31 := by
  apply stepAt_resultExact document history replayEventState123
    (⟨.program ⟨214⟩, ⟨10050⟩⟩) Proof.Events000.exact123RawTerms (.finite 46) 122 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact123RawTermsValid]
    rfl

theorem replayChunk30 : ReplayChain document history replayState30 replayState31 :=
  .chunk 124 (by
    unfold replayRange
    rw [show replayState30.cursor = 120 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (120 ≤ 124) && decide (124 ≤ 107567) &&
        decide (124 - 120 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState30.cursor = 120 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (124 - 120) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep120]
    simp only [Option.bind_some]
    rw [replayStep121]
    simp only [Option.bind_some]
    rw [replayStep122]
    simp only [Option.bind_some]
    rw [replayStep123]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk31 : ReplayChain document history replayState31 replayState32 :=
  .chunk 128 (by rfl)

def replayEventState129 : ReplayState := ⟨129, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState130 : ReplayState := ⟨130, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState131 : ReplayState := ⟨131, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep128 : stepAt document history replayState32 = some replayEventState129 := by
  apply stepAt_resultExact document history replayState32
    (⟨.program ⟨214⟩, ⟨12795⟩⟩) Proof.Events000.exact128RawTerms (.finite 2116) 126 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact128RawTermsValid]
    rfl

theorem replayStep129 : stepAt document history replayEventState129 = some replayEventState130 := by
  rfl

theorem replayStep130 : stepAt document history replayEventState130 = some replayEventState131 := by
  rfl

theorem replayStep131 : stepAt document history replayEventState131 = some replayState33 := by
  rfl

theorem replayChunk32 : ReplayChain document history replayState32 replayState33 :=
  .chunk 132 (by
    unfold replayRange
    rw [show replayState32.cursor = 128 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (128 ≤ 132) && decide (132 ≤ 107567) &&
        decide (132 - 128 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState32.cursor = 128 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (132 - 128) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep128]
    simp only [Option.bind_some]
    rw [replayStep129]
    simp only [Option.bind_some]
    rw [replayStep130]
    simp only [Option.bind_some]
    rw [replayStep131]
    simp only [Option.bind_some]
    rfl)

def replayEventState133 : ReplayState := ⟨133, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState134 : ReplayState := ⟨134, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState135 : ReplayState := ⟨135, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep132 : stepAt document history replayState33 = some replayEventState133 := by
  rfl

theorem replayStep133 : stepAt document history replayEventState133 = some replayEventState134 := by
  rfl

theorem replayStep134 : stepAt document history replayEventState134 = some replayEventState135 := by
  apply stepAt_resultExact document history replayEventState134
    (⟨.program ⟨214⟩, ⟨16649⟩⟩) Proof.Events000.exact134RawTerms (.finite 46) 133 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact134RawTermsValid]
    rfl

theorem replayStep135 : stepAt document history replayEventState135 = some replayState34 := by
  rfl

theorem replayChunk33 : ReplayChain document history replayState33 replayState34 :=
  .chunk 136 (by
    unfold replayRange
    rw [show replayState33.cursor = 132 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (132 ≤ 136) && decide (136 ≤ 107567) &&
        decide (136 - 132 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState33.cursor = 132 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (136 - 132) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep132]
    simp only [Option.bind_some]
    rw [replayStep133]
    simp only [Option.bind_some]
    rw [replayStep134]
    simp only [Option.bind_some]
    rw [replayStep135]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk34 : ReplayChain document history replayState34 replayState35 :=
  .chunk 140 (by rfl)

def replayEventState141 : ReplayState := ⟨141, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState142 : ReplayState := ⟨142, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState143 : ReplayState := ⟨143, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep140 : stepAt document history replayState35 = some replayEventState141 := by
  apply stepAt_resultExact document history replayState35
    (⟨.program ⟨214⟩, ⟨16691⟩⟩) Proof.Events000.exact140RawTerms (.finite 63) 139 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact140RawTermsValid]
    rfl

theorem replayStep141 : stepAt document history replayEventState141 = some replayEventState142 := by
  rfl

theorem replayStep142 : stepAt document history replayEventState142 = some replayEventState143 := by
  rfl

theorem replayStep143 : stepAt document history replayEventState143 = some replayState36 := by
  apply stepAt_resultExact document history replayEventState143
    (⟨.program ⟨214⟩, ⟨12598⟩⟩) Proof.Events000.exact143RawTerms (.finite 42) 142 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact143RawTermsValid]
    rfl

theorem replayChunk35 : ReplayChain document history replayState35 replayState36 :=
  .chunk 144 (by
    unfold replayRange
    rw [show replayState35.cursor = 140 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (140 ≤ 144) && decide (144 ≤ 107567) &&
        decide (144 - 140 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState35.cursor = 140 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (144 - 140) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep140]
    simp only [Option.bind_some]
    rw [replayStep141]
    simp only [Option.bind_some]
    rw [replayStep142]
    simp only [Option.bind_some]
    rw [replayStep143]
    simp only [Option.bind_some]
    rfl)

def replayEventState145 : ReplayState := ⟨145, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState146 : ReplayState := ⟨146, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState147 : ReplayState := ⟨147, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep144 : stepAt document history replayState36 = some replayEventState145 := by
  rfl

theorem replayStep145 : stepAt document history replayEventState145 = some replayEventState146 := by
  rfl

theorem replayStep146 : stepAt document history replayEventState146 = some replayEventState147 := by
  apply stepAt_resultExact document history replayEventState146
    (⟨.program ⟨214⟩, ⟨9945⟩⟩) Proof.Events000.exact146RawTerms (.finite 42) 145 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact146RawTermsValid]
    rfl

theorem replayStep147 : stepAt document history replayEventState147 = some replayState37 := by
  rfl

theorem replayChunk36 : ReplayChain document history replayState36 replayState37 :=
  .chunk 148 (by
    unfold replayRange
    rw [show replayState36.cursor = 144 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (144 ≤ 148) && decide (148 ≤ 107567) &&
        decide (148 - 144 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState36.cursor = 144 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (148 - 144) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep144]
    simp only [Option.bind_some]
    rw [replayStep145]
    simp only [Option.bind_some]
    rw [replayStep146]
    simp only [Option.bind_some]
    rw [replayStep147]
    simp only [Option.bind_some]
    rfl)

def replayEventState149 : ReplayState := ⟨149, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState150 : ReplayState := ⟨150, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState151 : ReplayState := ⟨151, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep148 : stepAt document history replayState37 = some replayEventState149 := by
  rfl

theorem replayStep149 : stepAt document history replayEventState149 = some replayEventState150 := by
  rfl

theorem replayStep150 : stepAt document history replayEventState150 = some replayEventState151 := by
  rfl

theorem replayStep151 : stepAt document history replayEventState151 = some replayState38 := by
  apply stepAt_resultExact document history replayEventState151
    (⟨.program ⟨214⟩, ⟨12599⟩⟩) Proof.Events000.exact151RawTerms (.finite 1764) 149 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact151RawTermsValid]
    rfl

theorem replayChunk37 : ReplayChain document history replayState37 replayState38 :=
  .chunk 152 (by
    unfold replayRange
    rw [show replayState37.cursor = 148 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (148 ≤ 152) && decide (152 ≤ 107567) &&
        decide (152 - 148 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState37.cursor = 148 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (152 - 148) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep148]
    simp only [Option.bind_some]
    rw [replayStep149]
    simp only [Option.bind_some]
    rw [replayStep150]
    simp only [Option.bind_some]
    rw [replayStep151]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk38 : ReplayChain document history replayState38 replayState39 :=
  .chunk 156 (by rfl)

def replayEventState157 : ReplayState := ⟨157, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState158 : ReplayState := ⟨158, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState159 : ReplayState := ⟨159, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep156 : stepAt document history replayState39 = some replayEventState157 := by
  rfl

theorem replayStep157 : stepAt document history replayEventState157 = some replayEventState158 := by
  apply stepAt_resultExact document history replayEventState157
    (⟨.program ⟨214⟩, ⟨16565⟩⟩) Proof.Events000.exact157RawTerms (.finite 42) 156 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact157RawTermsValid]
    rfl

theorem replayStep158 : stepAt document history replayEventState158 = some replayEventState159 := by
  rfl

theorem replayStep159 : stepAt document history replayEventState159 = some replayState40 := by
  rfl

theorem replayChunk39 : ReplayChain document history replayState39 replayState40 :=
  .chunk 160 (by
    unfold replayRange
    rw [show replayState39.cursor = 156 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (156 ≤ 160) && decide (160 ≤ 107567) &&
        decide (160 - 156 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState39.cursor = 156 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (160 - 156) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep156]
    simp only [Option.bind_some]
    rw [replayStep157]
    simp only [Option.bind_some]
    rw [replayStep158]
    simp only [Option.bind_some]
    rw [replayStep159]
    simp only [Option.bind_some]
    rfl)

def replayEventState161 : ReplayState := ⟨161, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState162 : ReplayState := ⟨162, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState163 : ReplayState := ⟨163, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep160 : stepAt document history replayState40 = some replayEventState161 := by
  rfl

theorem replayStep161 : stepAt document history replayEventState161 = some replayEventState162 := by
  rfl

theorem replayStep162 : stepAt document history replayEventState162 = some replayEventState163 := by
  rfl

theorem replayStep163 : stepAt document history replayEventState163 = some replayState41 := by
  apply stepAt_resultExact document history replayEventState163
    (⟨.program ⟨214⟩, ⟨18217⟩⟩) Proof.Events000.exact163RawTerms (.finite 63) 162 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact163RawTermsValid]
    rfl

theorem replayChunk40 : ReplayChain document history replayState40 replayState41 :=
  .chunk 164 (by
    unfold replayRange
    rw [show replayState40.cursor = 160 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (160 ≤ 164) && decide (164 ≤ 107567) &&
        decide (164 - 160 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState40.cursor = 160 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (164 - 160) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep160]
    simp only [Option.bind_some]
    rw [replayStep161]
    simp only [Option.bind_some]
    rw [replayStep162]
    simp only [Option.bind_some]
    rw [replayStep163]
    simp only [Option.bind_some]
    rfl)

def replayEventState165 : ReplayState := ⟨165, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState166 : ReplayState := ⟨166, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState167 : ReplayState := ⟨167, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep164 : stepAt document history replayState41 = some replayEventState165 := by
  rfl

theorem replayStep165 : stepAt document history replayEventState165 = some replayEventState166 := by
  rfl

theorem replayStep166 : stepAt document history replayEventState166 = some replayEventState167 := by
  apply stepAt_resultExact document history replayEventState166
    (⟨.program ⟨214⟩, ⟨12402⟩⟩) Proof.Events000.exact166RawTerms (.finite 40) 165 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact166RawTermsValid]
    rfl

theorem replayStep167 : stepAt document history replayEventState167 = some replayState42 := by
  rfl

theorem replayChunk41 : ReplayChain document history replayState41 replayState42 :=
  .chunk 168 (by
    unfold replayRange
    rw [show replayState41.cursor = 164 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (164 ≤ 168) && decide (168 ≤ 107567) &&
        decide (168 - 164 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState41.cursor = 164 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (168 - 164) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep164]
    simp only [Option.bind_some]
    rw [replayStep165]
    simp only [Option.bind_some]
    rw [replayStep166]
    simp only [Option.bind_some]
    rw [replayStep167]
    simp only [Option.bind_some]
    rfl)

def replayEventState169 : ReplayState := ⟨169, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState170 : ReplayState := ⟨170, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState171 : ReplayState := ⟨171, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep168 : stepAt document history replayState42 = some replayEventState169 := by
  rfl

theorem replayStep169 : stepAt document history replayEventState169 = some replayEventState170 := by
  apply stepAt_resultExact document history replayEventState169
    (⟨.program ⟨214⟩, ⟨9840⟩⟩) Proof.Events000.exact169RawTerms (.finite 40) 168 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact169RawTermsValid]
    rfl

theorem replayStep170 : stepAt document history replayEventState170 = some replayEventState171 := by
  rfl

theorem replayStep171 : stepAt document history replayEventState171 = some replayState43 := by
  rfl

theorem replayChunk42 : ReplayChain document history replayState42 replayState43 :=
  .chunk 172 (by
    unfold replayRange
    rw [show replayState42.cursor = 168 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (168 ≤ 172) && decide (172 ≤ 107567) &&
        decide (172 - 168 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState42.cursor = 168 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (172 - 168) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep168]
    simp only [Option.bind_some]
    rw [replayStep169]
    simp only [Option.bind_some]
    rw [replayStep170]
    simp only [Option.bind_some]
    rw [replayStep171]
    simp only [Option.bind_some]
    rfl)

def replayEventState173 : ReplayState := ⟨173, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState174 : ReplayState := ⟨174, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState175 : ReplayState := ⟨175, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep172 : stepAt document history replayState43 = some replayEventState173 := by
  rfl

theorem replayStep173 : stepAt document history replayEventState173 = some replayEventState174 := by
  rfl

theorem replayStep174 : stepAt document history replayEventState174 = some replayEventState175 := by
  apply stepAt_resultExact document history replayEventState174
    (⟨.program ⟨214⟩, ⟨12403⟩⟩) Proof.Events000.exact174RawTerms (.finite 1600) 172 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact174RawTermsValid]
    rfl

theorem replayStep175 : stepAt document history replayEventState175 = some replayState44 := by
  rfl

theorem replayChunk43 : ReplayChain document history replayState43 replayState44 :=
  .chunk 176 (by
    unfold replayRange
    rw [show replayState43.cursor = 172 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (172 ≤ 176) && decide (176 ≤ 107567) &&
        decide (176 - 172 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState43.cursor = 172 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (176 - 172) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep172]
    simp only [Option.bind_some]
    rw [replayStep173]
    simp only [Option.bind_some]
    rw [replayStep174]
    simp only [Option.bind_some]
    rw [replayStep175]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk44 : ReplayChain document history replayState44 replayState45 :=
  .chunk 180 (by rfl)

def replayEventState181 : ReplayState := ⟨181, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState182 : ReplayState := ⟨182, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState183 : ReplayState := ⟨183, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep180 : stepAt document history replayState45 = some replayEventState181 := by
  apply stepAt_resultExact document history replayState45
    (⟨.program ⟨214⟩, ⟨16481⟩⟩) Proof.Events000.exact180RawTerms (.finite 40) 179 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact180RawTermsValid]
    rfl

theorem replayStep181 : stepAt document history replayEventState181 = some replayEventState182 := by
  rfl

theorem replayStep182 : stepAt document history replayEventState182 = some replayEventState183 := by
  rfl

theorem replayStep183 : stepAt document history replayEventState183 = some replayState46 := by
  rfl

theorem replayChunk45 : ReplayChain document history replayState45 replayState46 :=
  .chunk 184 (by
    unfold replayRange
    rw [show replayState45.cursor = 180 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (180 ≤ 184) && decide (184 ≤ 107567) &&
        decide (184 - 180 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState45.cursor = 180 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (184 - 180) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep180]
    simp only [Option.bind_some]
    rw [replayStep181]
    simp only [Option.bind_some]
    rw [replayStep182]
    simp only [Option.bind_some]
    rw [replayStep183]
    simp only [Option.bind_some]
    rfl)

def replayEventState185 : ReplayState := ⟨185, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState186 : ReplayState := ⟨186, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState187 : ReplayState := ⟨187, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep184 : stepAt document history replayState46 = some replayEventState185 := by
  rfl

theorem replayStep185 : stepAt document history replayEventState185 = some replayEventState186 := by
  rfl

theorem replayStep186 : stepAt document history replayEventState186 = some replayEventState187 := by
  apply stepAt_resultExact document history replayEventState186
    (⟨.program ⟨214⟩, ⟨17916⟩⟩) Proof.Events000.exact186RawTerms (.finite 62) 185 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact186RawTermsValid]
    rfl

theorem replayStep187 : stepAt document history replayEventState187 = some replayState47 := by
  rfl

theorem replayChunk46 : ReplayChain document history replayState46 replayState47 :=
  .chunk 188 (by
    unfold replayRange
    rw [show replayState46.cursor = 184 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (184 ≤ 188) && decide (188 ≤ 107567) &&
        decide (188 - 184 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState46.cursor = 184 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (188 - 184) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep184]
    simp only [Option.bind_some]
    rw [replayStep185]
    simp only [Option.bind_some]
    rw [replayStep186]
    simp only [Option.bind_some]
    rw [replayStep187]
    simp only [Option.bind_some]
    rfl)

def replayEventState189 : ReplayState := ⟨189, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState190 : ReplayState := ⟨190, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState191 : ReplayState := ⟨191, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep188 : stepAt document history replayState47 = some replayEventState189 := by
  rfl

theorem replayStep189 : stepAt document history replayEventState189 = some replayEventState190 := by
  apply stepAt_resultExact document history replayEventState189
    (⟨.program ⟨214⟩, ⟨11989⟩⟩) Proof.Events000.exact189RawTerms (.finite 36) 188 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact189RawTermsValid]
    rfl

theorem replayStep190 : stepAt document history replayEventState190 = some replayEventState191 := by
  rfl

theorem replayStep191 : stepAt document history replayEventState191 = some replayState48 := by
  rfl

theorem replayChunk47 : ReplayChain document history replayState47 replayState48 :=
  .chunk 192 (by
    unfold replayRange
    rw [show replayState47.cursor = 188 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (188 ≤ 192) && decide (192 ≤ 107567) &&
        decide (192 - 188 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState47.cursor = 188 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (192 - 188) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep188]
    simp only [Option.bind_some]
    rw [replayStep189]
    simp only [Option.bind_some]
    rw [replayStep190]
    simp only [Option.bind_some]
    rw [replayStep191]
    simp only [Option.bind_some]
    rfl)

def replayEventState193 : ReplayState := ⟨193, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState194 : ReplayState := ⟨194, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState195 : ReplayState := ⟨195, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep192 : stepAt document history replayState48 = some replayEventState193 := by
  apply stepAt_resultExact document history replayState48
    (⟨.program ⟨214⟩, ⟨9735⟩⟩) Proof.Events000.exact192RawTerms (.finite 36) 191 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact192RawTermsValid]
    rfl

theorem replayStep193 : stepAt document history replayEventState193 = some replayEventState194 := by
  rfl

theorem replayStep194 : stepAt document history replayEventState194 = some replayEventState195 := by
  rfl

theorem replayStep195 : stepAt document history replayEventState195 = some replayState49 := by
  rfl

theorem replayChunk48 : ReplayChain document history replayState48 replayState49 :=
  .chunk 196 (by
    unfold replayRange
    rw [show replayState48.cursor = 192 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (192 ≤ 196) && decide (196 ≤ 107567) &&
        decide (196 - 192 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState48.cursor = 192 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (196 - 192) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep192]
    simp only [Option.bind_some]
    rw [replayStep193]
    simp only [Option.bind_some]
    rw [replayStep194]
    simp only [Option.bind_some]
    rw [replayStep195]
    simp only [Option.bind_some]
    rfl)

def replayEventState197 : ReplayState := ⟨197, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState198 : ReplayState := ⟨198, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState199 : ReplayState := ⟨199, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep196 : stepAt document history replayState49 = some replayEventState197 := by
  rfl

theorem replayStep197 : stepAt document history replayEventState197 = some replayEventState198 := by
  apply stepAt_resultExact document history replayEventState197
    (⟨.program ⟨214⟩, ⟨11990⟩⟩) Proof.Events000.exact197RawTerms (.finite 1296) 195 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact197RawTermsValid]
    rfl

theorem replayStep198 : stepAt document history replayEventState198 = some replayEventState199 := by
  rfl

theorem replayStep199 : stepAt document history replayEventState199 = some replayState50 := by
  rfl

theorem replayChunk49 : ReplayChain document history replayState49 replayState50 :=
  .chunk 200 (by
    unfold replayRange
    rw [show replayState49.cursor = 196 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (196 ≤ 200) && decide (200 ≤ 107567) &&
        decide (200 - 196 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState49.cursor = 196 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (200 - 196) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep196]
    simp only [Option.bind_some]
    rw [replayStep197]
    simp only [Option.bind_some]
    rw [replayStep198]
    simp only [Option.bind_some]
    rw [replayStep199]
    simp only [Option.bind_some]
    rfl)

def replayEventState201 : ReplayState := ⟨201, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState202 : ReplayState := ⟨202, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState203 : ReplayState := ⟨203, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep200 : stepAt document history replayState50 = some replayEventState201 := by
  rfl

theorem replayStep201 : stepAt document history replayEventState201 = some replayEventState202 := by
  rfl

theorem replayStep202 : stepAt document history replayEventState202 = some replayEventState203 := by
  rfl

theorem replayStep203 : stepAt document history replayEventState203 = some replayState51 := by
  apply stepAt_resultExact document history replayEventState203
    (⟨.program ⟨214⟩, ⟨16397⟩⟩) Proof.Events000.exact203RawTerms (.finite 36) 202 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact203RawTermsValid]
    rfl

theorem replayChunk50 : ReplayChain document history replayState50 replayState51 :=
  .chunk 204 (by
    unfold replayRange
    rw [show replayState50.cursor = 200 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (200 ≤ 204) && decide (204 ≤ 107567) &&
        decide (204 - 200 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState50.cursor = 200 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (204 - 200) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep200]
    simp only [Option.bind_some]
    rw [replayStep201]
    simp only [Option.bind_some]
    rw [replayStep202]
    simp only [Option.bind_some]
    rw [replayStep203]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk51 : ReplayChain document history replayState51 replayState52 :=
  .chunk 208 (by rfl)

def replayEventState209 : ReplayState := ⟨209, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState210 : ReplayState := ⟨210, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState211 : ReplayState := ⟨211, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep208 : stepAt document history replayState52 = some replayEventState209 := by
  rfl

theorem replayStep209 : stepAt document history replayEventState209 = some replayEventState210 := by
  apply stepAt_resultExact document history replayEventState209
    (⟨.program ⟨214⟩, ⟨17132⟩⟩) Proof.Events000.exact209RawTerms (.finite 62) 208 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact209RawTermsValid]
    rfl

theorem replayStep210 : stepAt document history replayEventState210 = some replayEventState211 := by
  rfl

theorem replayStep211 : stepAt document history replayEventState211 = some replayState53 := by
  rfl

theorem replayChunk52 : ReplayChain document history replayState52 replayState53 :=
  .chunk 212 (by
    unfold replayRange
    rw [show replayState52.cursor = 208 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (208 ≤ 212) && decide (212 ≤ 107567) &&
        decide (212 - 208 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState52.cursor = 208 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (212 - 208) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep208]
    simp only [Option.bind_some]
    rw [replayStep209]
    simp only [Option.bind_some]
    rw [replayStep210]
    simp only [Option.bind_some]
    rw [replayStep211]
    simp only [Option.bind_some]
    rfl)

def replayEventState213 : ReplayState := ⟨213, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState214 : ReplayState := ⟨214, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState215 : ReplayState := ⟨215, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep212 : stepAt document history replayState53 = some replayEventState213 := by
  apply stepAt_resultExact document history replayState53
    (⟨.program ⟨214⟩, ⟨11793⟩⟩) Proof.Events000.exact212RawTerms (.finite 30) 211 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact212RawTermsValid]
    rfl

theorem replayStep213 : stepAt document history replayEventState213 = some replayEventState214 := by
  rfl

theorem replayStep214 : stepAt document history replayEventState214 = some replayEventState215 := by
  rfl

theorem replayStep215 : stepAt document history replayEventState215 = some replayState54 := by
  apply stepAt_resultExact document history replayEventState215
    (⟨.program ⟨214⟩, ⟨9630⟩⟩) Proof.Events000.exact215RawTerms (.finite 30) 214 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact215RawTermsValid]
    rfl

theorem replayChunk53 : ReplayChain document history replayState53 replayState54 :=
  .chunk 216 (by
    unfold replayRange
    rw [show replayState53.cursor = 212 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (212 ≤ 216) && decide (216 ≤ 107567) &&
        decide (216 - 212 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState53.cursor = 212 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (216 - 212) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep212]
    simp only [Option.bind_some]
    rw [replayStep213]
    simp only [Option.bind_some]
    rw [replayStep214]
    simp only [Option.bind_some]
    rw [replayStep215]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk54 : ReplayChain document history replayState54 replayState55 :=
  .chunk 220 (by rfl)

def replayEventState221 : ReplayState := ⟨221, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState222 : ReplayState := ⟨222, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState223 : ReplayState := ⟨223, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep220 : stepAt document history replayState55 = some replayEventState221 := by
  apply stepAt_resultExact document history replayState55
    (⟨.program ⟨214⟩, ⟨11794⟩⟩) Proof.Events000.exact220RawTerms (.finite 900) 218 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact220RawTermsValid]
    rfl

theorem replayStep221 : stepAt document history replayEventState221 = some replayEventState222 := by
  rfl

theorem replayStep222 : stepAt document history replayEventState222 = some replayEventState223 := by
  rfl

theorem replayStep223 : stepAt document history replayEventState223 = some replayState56 := by
  rfl

theorem replayChunk55 : ReplayChain document history replayState55 replayState56 :=
  .chunk 224 (by
    unfold replayRange
    rw [show replayState55.cursor = 220 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (220 ≤ 224) && decide (224 ≤ 107567) &&
        decide (224 - 220 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState55.cursor = 220 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (224 - 220) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep220]
    simp only [Option.bind_some]
    rw [replayStep221]
    simp only [Option.bind_some]
    rw [replayStep222]
    simp only [Option.bind_some]
    rw [replayStep223]
    simp only [Option.bind_some]
    rfl)

def replayEventState225 : ReplayState := ⟨225, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState226 : ReplayState := ⟨226, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState227 : ReplayState := ⟨227, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep224 : stepAt document history replayState56 = some replayEventState225 := by
  rfl

theorem replayStep225 : stepAt document history replayEventState225 = some replayEventState226 := by
  rfl

theorem replayStep226 : stepAt document history replayEventState226 = some replayEventState227 := by
  apply stepAt_resultExact document history replayEventState226
    (⟨.program ⟨214⟩, ⟨16278⟩⟩) Proof.Events000.exact226RawTerms (.finite 30) 225 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact226RawTermsValid]
    rfl

theorem replayStep227 : stepAt document history replayEventState227 = some replayState57 := by
  rfl

theorem replayChunk56 : ReplayChain document history replayState56 replayState57 :=
  .chunk 228 (by
    unfold replayRange
    rw [show replayState56.cursor = 224 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (224 ≤ 228) && decide (228 ≤ 107567) &&
        decide (228 - 224 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState56.cursor = 224 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (228 - 224) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep224]
    simp only [Option.bind_some]
    rw [replayStep225]
    simp only [Option.bind_some]
    rw [replayStep226]
    simp only [Option.bind_some]
    rw [replayStep227]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk57 : ReplayChain document history replayState57 replayState58 :=
  .chunk 232 (by rfl)

def replayEventState233 : ReplayState := ⟨233, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState234 : ReplayState := ⟨234, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState235 : ReplayState := ⟨235, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep232 : stepAt document history replayState58 = some replayEventState233 := by
  apply stepAt_resultExact document history replayState58
    (⟨.program ⟨214⟩, ⟨16320⟩⟩) Proof.Events000.exact232RawTerms (.finite 62) 231 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact232RawTermsValid]
    rfl

theorem replayStep233 : stepAt document history replayEventState233 = some replayEventState234 := by
  rfl

theorem replayStep234 : stepAt document history replayEventState234 = some replayEventState235 := by
  rfl

theorem replayStep235 : stepAt document history replayEventState235 = some replayState59 := by
  apply stepAt_resultExact document history replayEventState235
    (⟨.program ⟨214⟩, ⟨11653⟩⟩) Proof.Events000.exact235RawTerms (.finite 28) 234 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact235RawTermsValid]
    rfl

theorem replayChunk58 : ReplayChain document history replayState58 replayState59 :=
  .chunk 236 (by
    unfold replayRange
    rw [show replayState58.cursor = 232 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (232 ≤ 236) && decide (236 ≤ 107567) &&
        decide (236 - 232 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState58.cursor = 232 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (236 - 232) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep232]
    simp only [Option.bind_some]
    rw [replayStep233]
    simp only [Option.bind_some]
    rw [replayStep234]
    simp only [Option.bind_some]
    rw [replayStep235]
    simp only [Option.bind_some]
    rfl)

def replayEventState237 : ReplayState := ⟨237, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState238 : ReplayState := ⟨238, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState239 : ReplayState := ⟨239, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep236 : stepAt document history replayState59 = some replayEventState237 := by
  rfl

theorem replayStep237 : stepAt document history replayEventState237 = some replayEventState238 := by
  rfl

theorem replayStep238 : stepAt document history replayEventState238 = some replayEventState239 := by
  apply stepAt_resultExact document history replayEventState238
    (⟨.program ⟨214⟩, ⟨14677⟩⟩) Proof.Events000.exact238RawTerms (.finite 28) 237 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact238RawTermsValid]
    rfl

theorem replayStep239 : stepAt document history replayEventState239 = some replayState60 := by
  rfl

theorem replayChunk59 : ReplayChain document history replayState59 replayState60 :=
  .chunk 240 (by
    unfold replayRange
    rw [show replayState59.cursor = 236 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (236 ≤ 240) && decide (240 ≤ 107567) &&
        decide (240 - 236 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState59.cursor = 236 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (240 - 236) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep236]
    simp only [Option.bind_some]
    rw [replayStep237]
    simp only [Option.bind_some]
    rw [replayStep238]
    simp only [Option.bind_some]
    rw [replayStep239]
    simp only [Option.bind_some]
    rfl)

def replayEventState241 : ReplayState := ⟨241, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState242 : ReplayState := ⟨242, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState243 : ReplayState := ⟨243, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep240 : stepAt document history replayState60 = some replayEventState241 := by
  rfl

theorem replayStep241 : stepAt document history replayEventState241 = some replayEventState242 := by
  rfl

theorem replayStep242 : stepAt document history replayEventState242 = some replayEventState243 := by
  rfl

theorem replayStep243 : stepAt document history replayEventState243 = some replayState61 := by
  apply stepAt_resultExact document history replayEventState243
    (⟨.program ⟨214⟩, ⟨14678⟩⟩) Proof.Events000.exact243RawTerms (.finite 784) 241 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact243RawTermsValid]
    rfl

theorem replayChunk60 : ReplayChain document history replayState60 replayState61 :=
  .chunk 244 (by
    unfold replayRange
    rw [show replayState60.cursor = 240 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (240 ≤ 244) && decide (244 ≤ 107567) &&
        decide (244 - 240 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState60.cursor = 240 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (244 - 240) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep240]
    simp only [Option.bind_some]
    rw [replayStep241]
    simp only [Option.bind_some]
    rw [replayStep242]
    simp only [Option.bind_some]
    rw [replayStep243]
    simp only [Option.bind_some]
    rfl)

theorem replayChunk61 : ReplayChain document history replayState61 replayState62 :=
  .chunk 248 (by rfl)

def replayEventState249 : ReplayState := ⟨249, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState250 : ReplayState := ⟨250, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState251 : ReplayState := ⟨251, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep248 : stepAt document history replayState62 = some replayEventState249 := by
  rfl

theorem replayStep249 : stepAt document history replayEventState249 = some replayEventState250 := by
  apply stepAt_resultExact document history replayEventState249
    (⟨.program ⟨214⟩, ⟨16194⟩⟩) Proof.Events000.exact249RawTerms (.finite 28) 248 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact249RawTermsValid]
    rfl

theorem replayStep250 : stepAt document history replayEventState250 = some replayEventState251 := by
  rfl

theorem replayStep251 : stepAt document history replayEventState251 = some replayState63 := by
  rfl

theorem replayChunk62 : ReplayChain document history replayState62 replayState63 :=
  .chunk 252 (by
    unfold replayRange
    rw [show replayState62.cursor = 248 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (248 ≤ 252) && decide (252 ≤ 107567) &&
        decide (252 - 248 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState62.cursor = 248 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (252 - 248) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep248]
    simp only [Option.bind_some]
    rw [replayStep249]
    simp only [Option.bind_some]
    rw [replayStep250]
    simp only [Option.bind_some]
    rw [replayStep251]
    simp only [Option.bind_some]
    rfl)

def replayEventState253 : ReplayState := ⟨253, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState254 : ReplayState := ⟨254, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩
def replayEventState255 : ReplayState := ⟨255, [⟨⟨.program ⟨214⟩, ⟨30220⟩⟩, 0⟩]⟩

theorem replayStep252 : stepAt document history replayState63 = some replayEventState253 := by
  rfl

theorem replayStep253 : stepAt document history replayEventState253 = some replayEventState254 := by
  rfl

theorem replayStep254 : stepAt document history replayEventState254 = some replayEventState255 := by
  rfl

theorem replayStep255 : stepAt document history replayEventState255 = some replayState64 := by
  apply stepAt_resultExact document history replayEventState255
    (⟨.program ⟨214⟩, ⟨18392⟩⟩) Proof.Events000.exact255RawTerms (.finite 62) 254 .exactZero (none) 0
  · rfl
  · rfl
  · rw [Proof.Events000.exact255RawTermsValid]
    rfl

theorem replayChunk63 : ReplayChain document history replayState63 replayState64 :=
  .chunk 256 (by
    unfold replayRange
    rw [show replayState63.cursor = 252 by rfl]
    rw [show history.size = 107567 by rfl]
    dsimp only
    rw [show (decide (252 ≤ 256) && decide (256 ≤ 107567) &&
        decide (256 - 252 ≤ 4)) = true by decide]
    simp only [if_true]
    unfold replayBlock
    rw [show replayState63.cursor = 252 by rfl]
    unfold replayBlock.run
    dsimp only
    rw [show Nat.min (256 - 252) 4 = 4 by decide]
    simp only [replayBlock.run, Option.bind_eq_bind]
    rw [replayStep252]
    simp only [Option.bind_some]
    rw [replayStep253]
    simp only [Option.bind_some]
    rw [replayStep254]
    simp only [Option.bind_some]
    rw [replayStep255]
    simp only [Option.bind_some]
    rfl)

theorem replayShard0 : ReplayChain document history replayState0 replayState64 :=
  (.trans (.trans (.trans (.trans (.trans (.trans replayChunk0 replayChunk1) (.trans replayChunk2 replayChunk3)) (.trans (.trans replayChunk4 replayChunk5) (.trans replayChunk6 replayChunk7))) (.trans (.trans (.trans replayChunk8 replayChunk9) (.trans replayChunk10 replayChunk11)) (.trans (.trans replayChunk12 replayChunk13) (.trans replayChunk14 replayChunk15)))) (.trans (.trans (.trans (.trans replayChunk16 replayChunk17) (.trans replayChunk18 replayChunk19)) (.trans (.trans replayChunk20 replayChunk21) (.trans replayChunk22 replayChunk23))) (.trans (.trans (.trans replayChunk24 replayChunk25) (.trans replayChunk26 replayChunk27)) (.trans (.trans replayChunk28 replayChunk29) (.trans replayChunk30 replayChunk31))))) (.trans (.trans (.trans (.trans (.trans replayChunk32 replayChunk33) (.trans replayChunk34 replayChunk35)) (.trans (.trans replayChunk36 replayChunk37) (.trans replayChunk38 replayChunk39))) (.trans (.trans (.trans replayChunk40 replayChunk41) (.trans replayChunk42 replayChunk43)) (.trans (.trans replayChunk44 replayChunk45) (.trans replayChunk46 replayChunk47)))) (.trans (.trans (.trans (.trans replayChunk48 replayChunk49) (.trans replayChunk50 replayChunk51)) (.trans (.trans replayChunk52 replayChunk53) (.trans replayChunk54 replayChunk55))) (.trans (.trans (.trans replayChunk56 replayChunk57) (.trans replayChunk58 replayChunk59)) (.trans (.trans replayChunk60 replayChunk61) (.trans replayChunk62 replayChunk63))))))

end Mxx.Certificate.OperationalNoise.TallSecurity0Generated.Proof.Replay000
