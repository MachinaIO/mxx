import RuntimePrimitives
import RuntimeMatrixOps
import RuntimeHash

/-! Concrete successful-sampling relations used by generated scope relations.

The runtime package contains only operation semantics.  It does not contain a graph, an
application-specific interpretation, or a second source of expected values. -/
